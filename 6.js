function tornarPositivo(numeroNegativo) {
    const numeroPositivo = Math.abs(numeroNegativo);
    console.log(`O número positivo correspondente é: ${numeroPositivo}`);
    return numeroPositivo;
  }
  
  tornarPositivo(-7);
  